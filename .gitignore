__pycache__/
*.py[cod]
*.pyo
.Python

# Training checkpoints (multi-GB; too large for GitHub)
outputs/**/checkpoints/
checkpoints/

# Wandb at repo root only (outputs/**/wandb/ is tracked)
/wandb/

# Logs outside outputs/
*.log
!outputs/**/*.log

# Results
results_*.jsonl

# CNN/DM datasets (too large for GitHub; keep locally)
data/cnndm_*.jsonl

# Hydra outputs (track run configs under outputs/)
multirun/
.hydra/
!outputs/**/.hydra/
!outputs/**/.hydra/**

# Editor / OS
.DS_Store
.vscode/
*.swp
